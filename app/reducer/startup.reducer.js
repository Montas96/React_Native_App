/* eslint-disable prettier/prettier */
export default function StartupReducer(state = {}, action) {
    switch (action.type) {
        case 'START_UP':
            return state;
        default:
            return state;
    }
}
